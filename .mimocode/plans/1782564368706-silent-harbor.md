# Plan: Fix status colors, statuses tab, warehouse-orders integration

## 1. Fix status badge text color — `static/css/style.css`
Add `color: #000` to light-background status classes (progress, delay) so text is visible. Keep white text on dark backgrounds.

## 2. Fix Statuses tab — `templates/status_list.html`, `app/routes.py`
Current page only manages client statuses. Add order statuses (`OrderStatus`) management on the same page — separate section with add/rename/delete.

## 3. Warehouse-Orders integration

### 3a. Model changes — `app/models.py`
- `OrderItem`: add `part_id` FK → `Part` (nullable, for linking warehouse parts)
- `OrderItem`: add `item_status` column (String, default "Ожидается") — states: "Ожидается", "В наличии", "Установлена", "Отменена"
- `Order`: add `payment_status` column (String, default "Не оплачен") — states: "Не оплачен", "Частично оплачен", "Оплачен"
- Migration: `flask db migrate` + `flask db upgrade`

### 3b. Order form — `templates/order_form.html`, `app/orders.py`
- Add dropdown to select warehouse part (auto-fills name, article, price)
- Add item_status selector per row
- Add payment_status selector on order level

### 3c. Order detail — `templates/order_detail.html`
- Show item_status badge per order item
- Show payment_status badge on order
- Show warehouse location for linked parts

### 3d. Order list — `templates/order_list.html`
- Show payment_status badge per order row

### 3e. Warehouse list — `templates/warehouse_list.html`
- Show how many orders use each part (link count)

## Files to modify
1. `static/css/style.css` — status badge colors
2. `app/models.py` — new columns
3. `app/routes.py` — order statuses in status_list
4. `templates/status_list.html` — order statuses UI
5. `app/orders.py` — handle new fields in create/edit
6. `templates/order_form.html` — warehouse part selector + item_status + payment_status
7. `templates/order_detail.html` — show statuses
8. `templates/order_list.html` — show payment_status

## Verification
1. Run app, check order detail — status text visible on all backgrounds
2. Statuses tab — manage both client and order statuses
3. Create order — select warehouse part, set item_status, payment_status
4. Order detail — verify all statuses display correctly
5. Warehouse — verify order count shown
